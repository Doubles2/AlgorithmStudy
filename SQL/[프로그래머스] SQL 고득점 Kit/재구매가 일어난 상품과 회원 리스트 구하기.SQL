-- 코드를 입력하세요
WITH BASE AS (
SELECT
    USER_ID,
    PRODUCT_ID,
    COUNT(SALES_AMOUNT) AS CNT
FROM
    ONLINE_SALE
GROUP BY
    1, 2
)
SELECT
    USER_ID,
    PRODUCT_ID
FROM
    BASE
WHERE
    CNT >= 2
ORDER BY
    USER_ID,
    PRODUCT_ID DESC